import {Events} from '../../../components/dashboard/events';
import {DashboardLayout} from '../../../components/dashboard/dashboard-layout';

export default function EventsPage() {
  return <Events />;
}

EventsPage.Layout = DashboardLayout;
